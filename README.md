# FlightApp
This is an airline management service called FlightApp that connects to a live Postgres database via Java + JDBC, including designing the schema for flights, users, and reservations and implementing the backend that powers the CLI workflow. Originally I worked on this through my school's server so you can't see all the exact timeline of my work!
